#ifndef RPM_H
#define RPM_H

volatile unsigned long rpm = 0;
volatile unsigned long lastTime = 0;
volatile unsigned long prevTime = 0; // Variable adicional para almacenar el tiempo de la interrupción anterior

#endif
